chrono-version: 10.0
type: TDPF
  
model:  "tdpf_model.yaml"
solver: "tdpf_solver.yaml"

output:
  format: NONE
  mode: FRAMES
  fps: 20

visualization:
  update_fps: 30
  color_map:
    type: HEIGHT
    map: BLUE
    min: -1.5
    max: 1.5
  