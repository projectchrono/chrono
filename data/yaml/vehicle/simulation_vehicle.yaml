# ------------------------------------------------------------------------------
# Basic rigid MBS simulation
# ------------------------------------------------------------------------------

chrono-version: 9.0

simulation:
  contact_method: SMC
  
  time_step: 5e-4
  end_time: 10
  enforce_realtime: true
  
  integrator:
      type: Euler_implicit_linearized
  
  solver:
      type: Barzilai_Borwein
      max_iterations: 100
      overrelaxation_factor: 0.8
      sharpness_factor: 1.0
  
  output:
      type: NONE
      fps: 20
      output_directory: "."

  visualization:
      type: Model_file
      render_fps: 60
      enable_shadows: true
      camera:
          vertical: Z
          location: [2, 2, 1.8]
          target: [0, 0, 0.6]
