The `contrib` directory contains various scripts and utilities that may
be helpful for working with the code, but are not part of the main
distribution.

Examples include:

- code reformatting scripts
- installer generation files
- code statistics
